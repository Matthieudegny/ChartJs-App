const initialState = {
  products: ["1001","1002","2001","2002","2003","3001","3002"],
  timeVisiting: [51,52,35,25,13,13,4]
};

function produitsReducer(state = initialState, action) {
  switch (action.type) {
    
    default:
      return state;
  }
}

export default produitsReducer;

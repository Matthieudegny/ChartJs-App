const initialState = {
  idUser: ["client id24","client id25","client id26","client id27"],
  connexionsMonth: [2,1,5,0],
  viewedItems: [4,5,7,2]
};

function customersReducer(state = initialState, action) {
  switch (action.type) {
  
    default:
      return state;
  }
}

export default customersReducer;
